# 🧪 QA Python Automation: API + UI

Учебный проект по автоматизации тестирования.
Реализованы проверки как Backend (API), так и Frontend (UI) части.

Проект построен с использованием паттерна **Page Object Model (POM)** для UI-тестов, что обеспечивает легкость поддержки и читаемость кода.

## 🛠 Стек технологий
* **Язык:** Python 3.10+
* **Тестовый раннер:** Pytest
* **API:** Requests
* **UI:** Playwright
* **Отчеты:** Pytest-HTML

## 📂 Структура проекта
* `pages/` — Описание страниц (Page Objects). Логика взаимодействия с элементами вынесена сюда.
* `test_*.py` — Сами тесты. Содержат только бизнес-логику и проверки (Asserts).
